# registry_cleaner
一个用于清理注册表的工具

# 使用

右键`registry_cleaner.exe`使用管理员运行,按照软件提示输入即可,使用该软件存在危险,如果因为操作失误导致损失,概不负责

# 构建

```
git clone https://github.com/fallingmeteorite/registry_cleaner.git

cd registry_cleaner

Cargo build

```